# Fullscreen active window
bind = $mod, F, fullscreen

# Float/Unfloat active window
bind = $mod, G, togglefloating,

# Move/resize windows with mod + LMB/RMB and dragging
bindm = $mod Shift, mouse:272, movewindow

bind = $mod, F1, togglegroup,
bind = $mod, F2, moveintogroup, l
bind = $mod, F2, moveintogroup, r
bind = $mod, F2, moveintogroup, u
bind = $mod, F2, moveintogroup, d
bind = $mod, F3, changegroupactive, b
bind = $mod, grave, changegroupactive, b

bind = $mod, F4, fullscreenstate, 1 1
#bind = $mod, F3, movewindow, u
bind = $mod, F4, movewindow, l
