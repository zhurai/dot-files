# Kill focused window
bind = $mod, Q, killactive,

# Kill focused window
bind = $mod Shift, Q, exec, /home/zhurai/.bin/hyprland.keybind.script.kill9-window.sh

# ignore middle button paste
bind = , mouse:274, exec, ;

# Push-to-talk in Discord?
bindn = Shift,F12, exec, pactl set-source-mute alsa_input.usb-audio-technica_AT2020USB_-00.analog-stereo 0
bindr = Shift,F12, exec, pactl set-source-mute alsa_input.usb-audio-technica_AT2020USB_-00.analog-stereo 1
