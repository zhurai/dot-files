# Terminal
bind = $mod, Return, exec, $term1

# Launcher
bind = $mod, Space, exec, wofi --show drun,run

# Screenshot
bind = $mod, S, exec, slurp | grim -g - screenshot.png
bind = $mod Shift, S, exec, grim screenshot.png
